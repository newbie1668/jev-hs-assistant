# Disabled — was overwriting sources with incomplete chunks.
# Kept empty on purpose so path stays reserved.
name: Assemble sources from chunks (disabled)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
